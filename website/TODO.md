# TODO - Angular Responsive Multi-Page Website

## Plan (approved)
1. Create standalone page components:
   - HomePage, AboutPage, ServicesPage, WorkPage, ContactPage
2. Create shared layout components:
   - NavbarComponent, FooterComponent
3. Update routing in `src/app/app.routes.ts`:
   - `/` → Home
   - `/about` → About
   - `/services` → Services
   - `/work` → Work
   - `/contact` → Contact
4. Update `AppComponent` layout:
   - Render navbar, main/router outlet, footer
5. Add responsive styling in `AppComponent` CSS and shared/global styles if needed.
6. Run `ng serve` and verify navigation + responsive layout.

## Progress
- [ ] Create page components
- [ ] Create navbar + footer components
- [ ] Wire up routes
- [ ] Update AppComponent template
- [ ] Add responsive styles
- [ ] Test with ng serve

